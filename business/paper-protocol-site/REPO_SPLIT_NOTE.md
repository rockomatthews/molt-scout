# Repo split note

This directory is the source for the standalone repo:
- https://github.com/rockomatthews/mint-machine-site

Split method:
- `git subtree split --prefix business/paper-protocol-site -b split-mint-machine-site`

Deploy preference: one site = one repo = one Vercel project.
