# Next.js Webhook Receiver Template

Includes:
- raw body capture
- signature verification pattern (HMAC)
- timestamp tolerance / anti-replay
- idempotency key store interface

Wire it into your agent pipeline (queue worker) so webhook delivery is reliable.
